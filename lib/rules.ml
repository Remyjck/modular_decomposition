(*Work on tree nodes*)

(*atomic identity down - ai_down*)
let atomic_identity_down tree = tree

(*super switch down - ss_down*)
let super_switch_down tree = tree

(*prime down - p_down*)
let prime_down tree = tree
